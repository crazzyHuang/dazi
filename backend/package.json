{
  "name": "tongpin-dazi-backend",
  "version": "1.0.0",
  "description": "Backend microservices for TongPin DaZi social platform",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "start": "pnpm -r run start",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "clean": "pnpm -r exec rm -rf dist node_modules",
    "install:all": "pnpm install"
  },
  "keywords": ["microservices", "social", "backend"],
  "author": "TongPin DaZi Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}